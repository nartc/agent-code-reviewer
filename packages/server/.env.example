# Comma-separated directories to scan for git repositories
SCAN_ROOTS=/path/to/your/code

# Max directory depth when scanning for repos (default: 3)
# SCAN_MAX_DEPTH=3

# Server port (default: 3847)
# PORT=3847

# Database file path (default: .data/reviewer.db relative to server CWD)
# Set explicitly if running the MCP server from a different directory than the server.
# DB_PATH=
